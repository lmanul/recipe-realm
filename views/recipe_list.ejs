<div class="recipe-list">
  <% recipes.forEach((recipe)=> { %>
      <div class="recipe-item">
          <img src="/img/<%= recipe.id %>.jpg" loading="lazy" />
          <span><%= recipe.name %></span>
      </div>
  <% }) %>
</div>
